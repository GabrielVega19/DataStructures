The queue object holds the code for the queue object which allows for opaque object design. There are functions to manage the object like queue, service, front, initilize, destroy and a couple more 
The main object creates a queue and then enqueues 10 int's and then services them all off printing the front as it goes. 
